{
  "track": "T3",
  "arm": "hewn_full",
  "prompt_id": "body-size-rollout-plan",
  "run_index": 3,
  "arm_order_index": 0,
  "arm_order_full": [
    "hewn_full",
    "hewn_prompt_only",
    "baseline",
    "terse",
    "caveman_full_plus_ultra_directive",
    "caveman_full"
  ],
  "digest_hex": "83cf61101f93c83952a720a1c10bb2b89a0fe33cd226823e26742150e5132c55",
  "seed": "hewn-bench-v1",
  "timestamp": "2026-04-22T09:52:06.527122+00:00",
  "output_tokens_tiktoken": 996,
  "output_tokens_anthropic": 1518,
  "input_tokens_anthropic": 6,
  "cache_creation_input_tokens": 0,
  "cache_read_input_tokens": 42532,
  "total_input_tokens": 42538,
  "cache_state": "warm",
  "wallclock_ms": 37947.41418698686,
  "duration_ms": 31518,
  "duration_api_ms": 32726,
  "wrapper_overhead_ms": 5221.414186986862,
  "stop_reason": "end_turn",
  "num_turns": 1,
  "session_id": "018d1bc9-36de-4153-9432-8611dc007a5a",
  "total_cost_usd": 0.064984,
  "model_used": "claude-haiku-4-5-20251001,claude-opus-4-7",
  "result": "1. Trusts client-supplied identity header. `req.headers['x-user-id']` violates Section 2 rule 1 — any external `X-User-Id` must be stripped by `header_sanitize` before handlers run. Handler lets a caller impersonate any user. Fix: delete the read; use `req.user.sub` (or the internal `X-Internal-User-Id` set after `auth_jwt`). Never read `X-User-Id` from the ingress request.\n\n2. No authentication enforced on handler. Handler does not confirm `req.user` exists; if registered off the main pipeline it bypasses `auth_jwt` entirely. Fix: register under the edge pipeline in `src/edge/pipeline.ts` after `auth_jwt`, and assert `req.user` + required roles (e.g. `transfer:write`) before touching `txSvc`.\n\n3. No org-membership/authorization check. User's validated `org` claim (`X-Internal-Org-Id`) is never compared to `req.params.org_id` — cross-org transfer possible with any valid JWT. Fix: reject with `403` if `req.user.org !== req.params.org_id` (or whatever RBAC rule applies).\n\n4. No input validation on body. `from`, `to`, `amount` used raw — no type, range, currency, or same-org account check; `amount` could be negative, string, NaN, or absurdly large. Fix: schema validate (zod/ajv); require positive integer minor-units, known-currency, `from`/`to` UUIDs, `from !== to`.\n\n5. Not routed through mTLS egress / `route_dispatcher`. Handler calls `txSvc.transfer` directly, bypassing Section 3 pipeline steps 6–8 (`route_dispatcher`, `mTLS_egress`, `response_filter`). Fix: dispatch via the standard egress so Envoy mTLS, internal headers (`X-Internal-User-Id`, `X-Internal-Org-Id`, `X-Internal-Auth-Method`), and response filtering apply.\n\n6. No rate limiting consideration for a money-moving POST. Default POST limit is 30/min/IP (Section 4); a transfer endpoint almost certainly wants a tighter per-user+per-org limit in `config/rate_limits.json`. Fix: add a route-prefix entry with a stricter limit, keyed on trusted IP + user id, not raw IP alone.\n\n7. No error handling / `next(err)`. `await txSvc.transfer(...)` can throw — unhandled rejection leaks to default handler, risks stacktrace/internal detail in response (Section 2 rule 6, Section 11). Fix: `try/catch` → `next(err)`; rely on `errorHandler.ts` for RFC 7807 body with `request_id`.\n\n8. Response is not RFC 7807 / lacks `request_id`. `{ok: true}` on success is fine, but any error path must include `request_id` matching the OTel trace and no internals. Fix: ensure error responses go through `errorHandler`; include `request_id` in success body too if downstream audit expects it.\n\n9. Missing audit log for a financial action. Section 3 step 9 requires `audit_log`; funds transfer is exactly what the 365-day audit bucket (Section 9) exists for. Fix: emit a structured audit event with `user_id`, `org_id`, `from`, `to`, `amount`, `request_id`, `jti`, result.\n\n10. No idempotency key. Money-moving POST with retries will double-transfer on network blips. Fix: require `Idempotency-Key` header, dedupe in `tx-svc` or at edge via Redis `SETNX` with TTL.\n\n11. Body size / content-type not asserted. Relies on `body_size_guard` (2MB default, Section 3) which is fine, but handler should assert `application/json` and a tight per-route size (e.g. 4KB). Fix: route-level `express.json({limit: '4kb'})` or validator rejects oversized.\n\n12. PII risk in logs. `from`/`to` may be account numbers; if logged raw they should go through `pii_redact` (Section 2 rule 5). Fix: extend redactor field list or hash account ids before logging.\n\n13. CODEOWNERS / review gate. Adding a money-moving route and any change to auth/rate-limit config needs CODEOWNERS approval per Section 6. Fix: ensure the PR touches `config/rate_limits.json` and gets a required human approver; add integration test against real Redis+PG (Section 6, Section 11 — no mocks).",
  "assertion_pass": true,
  "raw_payload": {
    "type": "result",
    "subtype": "success",
    "is_error": false,
    "api_error_status": null,
    "duration_ms": 31518,
    "duration_api_ms": 32726,
    "num_turns": 1,
    "result": "1. Trusts client-supplied identity header. `req.headers['x-user-id']` violates Section 2 rule 1 — any external `X-User-Id` must be stripped by `header_sanitize` before handlers run. Handler lets a caller impersonate any user. Fix: delete the read; use `req.user.sub` (or the internal `X-Internal-User-Id` set after `auth_jwt`). Never read `X-User-Id` from the ingress request.\n\n2. No authentication enforced on handler. Handler does not confirm `req.user` exists; if registered off the main pipeline it bypasses `auth_jwt` entirely. Fix: register under the edge pipeline in `src/edge/pipeline.ts` after `auth_jwt`, and assert `req.user` + required roles (e.g. `transfer:write`) before touching `txSvc`.\n\n3. No org-membership/authorization check. User's validated `org` claim (`X-Internal-Org-Id`) is never compared to `req.params.org_id` — cross-org transfer possible with any valid JWT. Fix: reject with `403` if `req.user.org !== req.params.org_id` (or whatever RBAC rule applies).\n\n4. No input validation on body. `from`, `to`, `amount` used raw — no type, range, currency, or same-org account check; `amount` could be negative, string, NaN, or absurdly large. Fix: schema validate (zod/ajv); require positive integer minor-units, known-currency, `from`/`to` UUIDs, `from !== to`.\n\n5. Not routed through mTLS egress / `route_dispatcher`. Handler calls `txSvc.transfer` directly, bypassing Section 3 pipeline steps 6–8 (`route_dispatcher`, `mTLS_egress`, `response_filter`). Fix: dispatch via the standard egress so Envoy mTLS, internal headers (`X-Internal-User-Id`, `X-Internal-Org-Id`, `X-Internal-Auth-Method`), and response filtering apply.\n\n6. No rate limiting consideration for a money-moving POST. Default POST limit is 30/min/IP (Section 4); a transfer endpoint almost certainly wants a tighter per-user+per-org limit in `config/rate_limits.json`. Fix: add a route-prefix entry with a stricter limit, keyed on trusted IP + user id, not raw IP alone.\n\n7. No error handling / `next(err)`. `await txSvc.transfer(...)` can throw — unhandled rejection leaks to default handler, risks stacktrace/internal detail in response (Section 2 rule 6, Section 11). Fix: `try/catch` → `next(err)`; rely on `errorHandler.ts` for RFC 7807 body with `request_id`.\n\n8. Response is not RFC 7807 / lacks `request_id`. `{ok: true}` on success is fine, but any error path must include `request_id` matching the OTel trace and no internals. Fix: ensure error responses go through `errorHandler`; include `request_id` in success body too if downstream audit expects it.\n\n9. Missing audit log for a financial action. Section 3 step 9 requires `audit_log`; funds transfer is exactly what the 365-day audit bucket (Section 9) exists for. Fix: emit a structured audit event with `user_id`, `org_id`, `from`, `to`, `amount`, `request_id`, `jti`, result.\n\n10. No idempotency key. Money-moving POST with retries will double-transfer on network blips. Fix: require `Idempotency-Key` header, dedupe in `tx-svc` or at edge via Redis `SETNX` with TTL.\n\n11. Body size / content-type not asserted. Relies on `body_size_guard` (2MB default, Section 3) which is fine, but handler should assert `application/json` and a tight per-route size (e.g. 4KB). Fix: route-level `express.json({limit: '4kb'})` or validator rejects oversized.\n\n12. PII risk in logs. `from`/`to` may be account numbers; if logged raw they should go through `pii_redact` (Section 2 rule 5). Fix: extend redactor field list or hash account ids before logging.\n\n13. CODEOWNERS / review gate. Adding a money-moving route and any change to auth/rate-limit config needs CODEOWNERS approval per Section 6. Fix: ensure the PR touches `config/rate_limits.json` and gets a required human approver; add integration test against real Redis+PG (Section 6, Section 11 — no mocks).",
    "stop_reason": "end_turn",
    "session_id": "018d1bc9-36de-4153-9432-8611dc007a5a",
    "total_cost_usd": 0.064984,
    "usage": {
      "input_tokens": 6,
      "cache_creation_input_tokens": 0,
      "cache_read_input_tokens": 42532,
      "output_tokens": 1518,
      "server_tool_use": {
        "web_search_requests": 0,
        "web_fetch_requests": 0
      },
      "service_tier": "standard",
      "cache_creation": {
        "ephemeral_1h_input_tokens": 0,
        "ephemeral_5m_input_tokens": 0
      },
      "inference_geo": "",
      "iterations": [
        {
          "input_tokens": 6,
          "output_tokens": 1518,
          "cache_read_input_tokens": 42532,
          "cache_creation_input_tokens": 0,
          "cache_creation": {
            "ephemeral_5m_input_tokens": 0,
            "ephemeral_1h_input_tokens": 0
          },
          "type": "message"
        }
      ],
      "speed": "standard"
    },
    "modelUsage": {
      "claude-haiku-4-5-20251001": {
        "inputTokens": 5668,
        "outputTokens": 14,
        "cacheReadInputTokens": 0,
        "cacheCreationInputTokens": 0,
        "webSearchRequests": 0,
        "costUSD": 0.005738,
        "contextWindow": 200000,
        "maxOutputTokens": 32000
      },
      "claude-opus-4-7": {
        "inputTokens": 6,
        "outputTokens": 1518,
        "cacheReadInputTokens": 42532,
        "cacheCreationInputTokens": 0,
        "webSearchRequests": 0,
        "costUSD": 0.059246,
        "contextWindow": 1000000,
        "maxOutputTokens": 64000
      }
    },
    "permission_denials": [],
    "terminal_reason": "completed",
    "fast_mode_state": "off",
    "uuid": "0e8690a3-1302-480f-b207-11408d9b1f84"
  }
}