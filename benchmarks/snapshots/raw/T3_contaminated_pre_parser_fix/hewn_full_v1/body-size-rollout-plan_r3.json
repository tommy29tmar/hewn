{
  "track": "T3",
  "arm": "hewn_full",
  "prompt_id": "body-size-rollout-plan",
  "run_index": 3,
  "arm_order_index": 0,
  "arm_order_full": [
    "hewn_full",
    "hewn_prompt_only",
    "baseline",
    "terse",
    "caveman_full_plus_ultra_directive",
    "caveman_full"
  ],
  "digest_hex": "83cf61101f93c83952a720a1c10bb2b89a0fe33cd226823e26742150e5132c55",
  "seed": "hewn-bench-v1",
  "timestamp": "2026-04-22T00:49:19.629657+00:00",
  "output_tokens_tiktoken": 883,
  "output_tokens_anthropic": 1441,
  "input_tokens_anthropic": 6,
  "cache_creation_input_tokens": 24640,
  "cache_read_input_tokens": 16805,
  "total_input_tokens": 41451,
  "cache_state": "warm",
  "wallclock_ms": 29392.641474987613,
  "duration_ms": 26319,
  "duration_api_ms": 27415,
  "wrapper_overhead_ms": 1977.641474987613,
  "stop_reason": "end_turn",
  "num_turns": 1,
  "session_id": "ef092220-c3e2-4b31-86c2-83493e6fe22e",
  "total_cost_usd": 0.20421050000000002,
  "model_used": "claude-haiku-4-5-20251001,claude-opus-4-7",
  "result": "1. Trusts spoofable X-User-Id header for identity\nEvidence: `req.headers['x-user-id']` — Section 2 rule 1, Section 5\nImpact: any caller can impersonate any user, transfer funds from arbitrary accounts; P0 auth bypass\nFix: remove; use `req.user.sub` populated by `auth_jwt` middleware. Forward identity downstream only as `X-Internal-User-Id` over mTLS\n\n2. Handler registered outside pipeline ordering guarantees\nEvidence: `app.post(...)` direct registration — Section 3 pipeline\nImpact: bypasses `header_sanitize`, `rate_limit`, `body_size_guard`, `auth_jwt`, `audit_log` if mounted before them or on wrong router\nFix: mount under router that inherits the full middleware chain from `src/edge/pipeline.ts`; confirm `auth_jwt` ran (guard on `req.user`)\n\n3. No authorization check that user belongs to `org_id`\nEvidence: `orgId: req.params.org_id` passed straight through\nImpact: authenticated user A can transfer funds inside org B by changing URL param; horizontal privilege escalation\nFix: verify `req.user.org === req.params.org_id` (or role includes org), reject `403` otherwise; do not rely on tx-svc alone\n\n4. No input validation on body\nEvidence: `{ from, to, amount } = req.body` used unchecked\nImpact: negative/zero/NaN/string amounts, missing fields, injection into downstream; type confusion in tx-svc\nFix: schema-validate (zod/ajv) — `from`/`to` UUIDs, `amount` positive integer minor-units, reject unknown fields; `400` problem+json on fail\n\n5. No idempotency key on a money-moving POST\nEvidence: handler accepts no `Idempotency-Key` header, no dedup\nImpact: client retry or network replay double-transfers funds\nFix: require `Idempotency-Key` header, dedupe in Redis/tx-svc keyed by `(org_id, key)` with TTL; return prior result on replay\n\n6. No error handling — unhandled promise rejection\nEvidence: `await txSvc.transfer(...)` with no try/catch, no `next(err)`\nImpact: rejection escapes to default Express handler; leaks stacktrace, skips `response_filter` / `audit_log`; violates Section 2 rule 6 (RFC 7807)\nFix: wrap in try/catch → `next(err)` to `src/edge/errorHandler.ts`, or use async wrapper; return problem+json with `request_id`\n\n7. Success response not RFC 7807-shaped / missing request_id\nEvidence: `res.json({ok: true})` — Section 2 rule 6\nImpact: inconsistent contract; no trace correlation on client side for money ops\nFix: return `{ request_id, transfer_id, status }`; set `request_id` from OTel trace id\n\n8. Route not in rate-limit config / uses default POST limit (30/min/IP)\nEvidence: no entry in `config/rate_limits.json` implied\nImpact: 30/min/IP on a funds-transfer endpoint is too loose for abuse, too tight for legit batch; also keyed by IP only, not user/org\nFix: add explicit per-route limit keyed on `(user_id, org_id)` not just IP; tighter ceiling; coordinate with fraud team\n\n9. No audit-log entry for money movement\nEvidence: handler ends at `res.json`, no explicit audit call — Section 9 (365-day audit retention)\nImpact: financial action not in tamper-evident audit bucket; compliance gap\nFix: emit structured audit event (`action=transfer`, `org_id`, `user_id`, `from`, `to`, `amount`, `request_id`, `jti`) before response\n\n10. Missing CODEOWNERS-equivalent scrutiny / no tests\nEvidence: no test file referenced, handler added inline in `app.*`\nImpact: money-path change with no unit or integration coverage; Section 6 requires integration tests against real PG/Redis, not mocks\nFix: add integration tests for happy path, cross-org rejection, bad amount, idempotent replay, missing JWT, expired JWT",
  "assertion_pass": true,
  "raw_payload": {
    "type": "result",
    "subtype": "success",
    "is_error": false,
    "api_error_status": null,
    "duration_ms": 26319,
    "duration_api_ms": 27415,
    "num_turns": 1,
    "result": "1. Trusts spoofable X-User-Id header for identity\nEvidence: `req.headers['x-user-id']` — Section 2 rule 1, Section 5\nImpact: any caller can impersonate any user, transfer funds from arbitrary accounts; P0 auth bypass\nFix: remove; use `req.user.sub` populated by `auth_jwt` middleware. Forward identity downstream only as `X-Internal-User-Id` over mTLS\n\n2. Handler registered outside pipeline ordering guarantees\nEvidence: `app.post(...)` direct registration — Section 3 pipeline\nImpact: bypasses `header_sanitize`, `rate_limit`, `body_size_guard`, `auth_jwt`, `audit_log` if mounted before them or on wrong router\nFix: mount under router that inherits the full middleware chain from `src/edge/pipeline.ts`; confirm `auth_jwt` ran (guard on `req.user`)\n\n3. No authorization check that user belongs to `org_id`\nEvidence: `orgId: req.params.org_id` passed straight through\nImpact: authenticated user A can transfer funds inside org B by changing URL param; horizontal privilege escalation\nFix: verify `req.user.org === req.params.org_id` (or role includes org), reject `403` otherwise; do not rely on tx-svc alone\n\n4. No input validation on body\nEvidence: `{ from, to, amount } = req.body` used unchecked\nImpact: negative/zero/NaN/string amounts, missing fields, injection into downstream; type confusion in tx-svc\nFix: schema-validate (zod/ajv) — `from`/`to` UUIDs, `amount` positive integer minor-units, reject unknown fields; `400` problem+json on fail\n\n5. No idempotency key on a money-moving POST\nEvidence: handler accepts no `Idempotency-Key` header, no dedup\nImpact: client retry or network replay double-transfers funds\nFix: require `Idempotency-Key` header, dedupe in Redis/tx-svc keyed by `(org_id, key)` with TTL; return prior result on replay\n\n6. No error handling — unhandled promise rejection\nEvidence: `await txSvc.transfer(...)` with no try/catch, no `next(err)`\nImpact: rejection escapes to default Express handler; leaks stacktrace, skips `response_filter` / `audit_log`; violates Section 2 rule 6 (RFC 7807)\nFix: wrap in try/catch → `next(err)` to `src/edge/errorHandler.ts`, or use async wrapper; return problem+json with `request_id`\n\n7. Success response not RFC 7807-shaped / missing request_id\nEvidence: `res.json({ok: true})` — Section 2 rule 6\nImpact: inconsistent contract; no trace correlation on client side for money ops\nFix: return `{ request_id, transfer_id, status }`; set `request_id` from OTel trace id\n\n8. Route not in rate-limit config / uses default POST limit (30/min/IP)\nEvidence: no entry in `config/rate_limits.json` implied\nImpact: 30/min/IP on a funds-transfer endpoint is too loose for abuse, too tight for legit batch; also keyed by IP only, not user/org\nFix: add explicit per-route limit keyed on `(user_id, org_id)` not just IP; tighter ceiling; coordinate with fraud team\n\n9. No audit-log entry for money movement\nEvidence: handler ends at `res.json`, no explicit audit call — Section 9 (365-day audit retention)\nImpact: financial action not in tamper-evident audit bucket; compliance gap\nFix: emit structured audit event (`action=transfer`, `org_id`, `user_id`, `from`, `to`, `amount`, `request_id`, `jti`) before response\n\n10. Missing CODEOWNERS-equivalent scrutiny / no tests\nEvidence: no test file referenced, handler added inline in `app.*`\nImpact: money-path change with no unit or integration coverage; Section 6 requires integration tests against real PG/Redis, not mocks\nFix: add integration tests for happy path, cross-org rejection, bad amount, idempotent replay, missing JWT, expired JWT",
    "stop_reason": "end_turn",
    "session_id": "ef092220-c3e2-4b31-86c2-83493e6fe22e",
    "total_cost_usd": 0.20421050000000002,
    "usage": {
      "input_tokens": 6,
      "cache_creation_input_tokens": 24640,
      "cache_read_input_tokens": 16805,
      "output_tokens": 1441,
      "server_tool_use": {
        "web_search_requests": 0,
        "web_fetch_requests": 0
      },
      "service_tier": "standard",
      "cache_creation": {
        "ephemeral_1h_input_tokens": 24640,
        "ephemeral_5m_input_tokens": 0
      },
      "inference_geo": "",
      "iterations": [
        {
          "input_tokens": 6,
          "output_tokens": 1441,
          "cache_read_input_tokens": 16805,
          "cache_creation_input_tokens": 24640,
          "cache_creation": {
            "ephemeral_5m_input_tokens": 0,
            "ephemeral_1h_input_tokens": 24640
          },
          "type": "message"
        }
      ],
      "speed": "standard"
    },
    "modelUsage": {
      "claude-haiku-4-5-20251001": {
        "inputTokens": 5668,
        "outputTokens": 17,
        "cacheReadInputTokens": 0,
        "cacheCreationInputTokens": 0,
        "webSearchRequests": 0,
        "costUSD": 0.005753,
        "contextWindow": 200000,
        "maxOutputTokens": 32000
      },
      "claude-opus-4-7": {
        "inputTokens": 6,
        "outputTokens": 1441,
        "cacheReadInputTokens": 16805,
        "cacheCreationInputTokens": 24640,
        "webSearchRequests": 0,
        "costUSD": 0.1984575,
        "contextWindow": 1000000,
        "maxOutputTokens": 64000
      }
    },
    "permission_denials": [],
    "terminal_reason": "completed",
    "fast_mode_state": "off",
    "uuid": "b96b9c02-e781-4208-883a-bc72b779000c"
  }
}