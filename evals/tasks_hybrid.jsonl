{"id":"debug-auth-expiry","category":"debugging","mode":"hybrid","prompt":"You are reviewing this Express auth middleware bug. Requirements: preserve backward compatibility, preserve security, prefer the smallest possible diff, and add one regression test.\n\nCode:\n```js\nexport async function authMiddleware(req, res, next) {\n  const token = getBearerToken(req);\n  if (!token) return res.status(401).json({ error: 'missing_token' });\n\n  const payload = verifyJwt(token);\n  const nowMs = Date.now();\n  const expMs = payload.exp * 1000;\n\n  if (expMs < nowMs) {\n    return res.status(401).json({ error: 'expired_token' });\n  }\n\n  req.userId = payload.sub;\n  return next();\n}\n```\n\nObserved failure: tokens that expire exactly at the boundary sometimes trigger a 401 loop during refresh because another service allows a 30-second grace window for clock skew. Suggest the minimal fix and the regression test in the shortest correct form.","must_include":["auth","grace","test","regression"],"exact_literals":["<","401"]}
{"id":"arch-small-team","category":"architecture","mode":"hybrid","prompt":"Architecture decision. Context: team of 6, one part-time DevOps generalist, shipping in 4 months, modest initial traffic, one PostgreSQL database, likely future split between billing and core app, but not before first release. Choose the default architecture and justify it briefly.","must_include":["modular_monolith","team","deadline"],"exact_literals":["6","4 months","PostgreSQL"]}
{"id":"security-review-header-auth","category":"code_review","mode":"hybrid","prompt":"Review this patch for likely security regressions and give the shortest actionable report.\n\nDiff:\n```diff\n- const userId = session.userId;\n+ const userId = req.headers['x-user-id'] || session.userId;\n  if (!userId) return res.status(401).end();\n```\n\nContext: this handler is behind the public API gateway. Keep the report concise but include risk and verification.","must_include":["risk","verify","header","auth"],"exact_literals":["x-user-id","401"]}
{"id":"refactor-callback","category":"refactoring","mode":"hybrid","prompt":"Refactor this code to async/await with minimal behavioral change. Keep the same validation order and keep one place that maps database errors to `next(err)`.\n\nCode:\n```js\nfunction loadUser(id, next) {\n  if (!id) return next(new Error('missing_id'));\n  db.findUser(id, function(err, user) {\n    if (err) return next(err);\n    if (!user) return next(new Error('not_found'));\n    audit.log(user.id, function(logErr) {\n      if (logErr) return next(logErr);\n      cache.set(user.id, user, function(cacheErr) {\n        if (cacheErr) return next(cacheErr);\n        next(null, user);\n      });\n    });\n  });\n}\n```\n\nReturn the shortest correct migration plan or patch sketch.","must_include":["async","await","verify","minimal"],"exact_literals":["async","await","next(err)"]}
